default:
    extensions:
        Laracasts\Behat:
            # env_path: .env
        Behat\MinkExtension:
            default_session: laravel
            base_url: http://sanctions.demo.localflynn.com/
            laravel: ~
